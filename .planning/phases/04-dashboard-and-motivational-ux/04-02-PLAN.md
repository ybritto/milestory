---
phase: 04-dashboard-and-motivational-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - milestory-frontend/package.json
  - milestory-frontend/src/app/app.config.ts
  - milestory-frontend/src/app/app.routes.ts
  - milestory-frontend/src/app/app.html
  - milestory-frontend/src/app/app.spec.ts
  - milestory-frontend/src/app/shell/app-shell.component.ts
  - milestory-frontend/src/app/shell/app-shell.component.html
  - milestory-frontend/src/app/shell/app-shell.component.scss
  - milestory-frontend/src/app/shell/app-shell.component.spec.ts
  - milestory-frontend/src/app/features/goals/goal.routes.ts
  - milestory-frontend/src/app/features/goals/active/active-goals.page.ts
  - milestory-frontend/src/app/features/goals/active/active-goals.page.html
  - milestory-frontend/src/app/features/goals/active/active-goals.page.scss
  - milestory-frontend/src/app/features/goals/active/active-goals.page.spec.ts
  - milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts
  - milestory-frontend/src/app/features/goals/detail/goal-detail.page.html
  - milestory-frontend/src/app/features/goals/detail/goal-detail.page.scss
  - milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts
autonomous: true
requirements:
  - PLAT-03
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "User lands inside a real Milestory shell instead of the starter home placeholder."
    - "User can move between Dashboard, Active goals, Archive, and New goal from a persistent sidebar or mobile drawer."
    - "User can scan all active goals on a dedicated route and drill into goal detail without route confusion."
    - "User sees the same accomplishment-band treatment on goal detail that appears on the dashboard."
  artifacts:
    - path: "milestory-frontend/src/app/shell/app-shell.component.ts"
      provides: "Persistent app shell with sidebar and mobile drawer"
      contains: "export class AppShellComponent"
    - path: "milestory-frontend/src/app/features/goals/active/active-goals.page.ts"
      provides: "Dedicated active-goals route"
      contains: "export class ActiveGoalsPage"
    - path: "milestory-frontend/src/app/app.routes.ts"
      provides: "Shell-first route tree with dashboard as default entry"
      contains: "redirectTo: 'dashboard'"
    - path: "milestory-frontend/src/app/features/goals/detail/goal-detail.page.html"
      provides: "Goal detail accomplishment-band rendering"
      contains: "app-accomplishment-band"
  key_links:
    - from: "milestory-frontend/src/app/app.routes.ts"
      to: "milestory-frontend/src/app/shell/app-shell.component.ts"
      via: "root shell route"
      pattern: "loadComponent.*app-shell"
    - from: "milestory-frontend/src/app/shell/app-shell.component.html"
      to: "milestory-frontend/src/app/features/dashboard/dashboard.page.ts"
      via: "dashboard nav link and child outlet"
      pattern: "routerLink=.*dashboard"
    - from: "milestory-frontend/src/app/features/goals/active/active-goals.page.html"
      to: "milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.ts"
      via: "shared goal summary cards"
      pattern: "app-goal-summary-card"
---

<objective>
Replace the placeholder root experience with the real Milestory shell and goal navigation surfaces that consume the Phase 4 dashboard UI.

Purpose: Satisfy PLAT-03 and complete the dashboard drill-down loop by making the shell, active-goals route, and goal-detail accomplishment treatment coherent across desktop and mobile.
Output: A tested shell with sidebar/drawer navigation, dashboard as the default entry route, a dedicated active-goals page, and aligned accomplishment rendering in goal detail.
</objective>

<execution_context>
@/Users/ybritto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ybritto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-and-motivational-ux/04-CONTEXT.md
@.planning/phases/04-dashboard-and-motivational-ux/04-RESEARCH.md
@.planning/phases/04-dashboard-and-motivational-ux/04-VALIDATION.md
@.planning/phases/04-dashboard-and-motivational-ux/04-01-PLAN.md
@milestory-frontend/src/app/app.routes.ts
@milestory-frontend/src/app/app.html
@milestory-frontend/src/app/app.config.ts
@milestory-frontend/src/app/features/goals/goal.routes.ts
@milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts
@milestory-frontend/src/app/features/goals/archive/goal-archive.page.ts
@milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.ts
@milestory-frontend/src/app/shared/ui/accomplishment-band/accomplishment-band.component.ts

<interfaces>
From `milestory-frontend/src/app/features/goals/goal.routes.ts`:
```typescript
export const GOAL_ROUTES: Routes = [
  { path: '', pathMatch: 'full', redirectTo: 'new' },
  { path: 'new', ... },
  { path: 'archive', ... },
  { path: ':goalId', ... },
];
```

From `milestory-frontend/src/app/features/dashboard/dashboard.page.ts` after Plan 04-01:
```typescript
export class DashboardPage {}
```

Locked decisions to implement:
- Per D-04, the shell must expose `Dashboard`, `Active goals`, `Archive`, and a strong `New goal` action.
- Per D-04, the sidebar stays visible on desktop and collapses into a drawer on mobile/tablet.
- Per D-04, the old home page stops being the primary shipped destination once the shell exists.
- Deferred: no dashboard-side progress logging or checkpoint editing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the shell-first route tree and responsive app shell</name>
  <files>milestory-frontend/package.json, milestory-frontend/src/app/app.config.ts, milestory-frontend/src/app/app.routes.ts, milestory-frontend/src/app/app.html, milestory-frontend/src/app/app.spec.ts, milestory-frontend/src/app/shell/app-shell.component.ts, milestory-frontend/src/app/shell/app-shell.component.html, milestory-frontend/src/app/shell/app-shell.component.scss, milestory-frontend/src/app/shell/app-shell.component.spec.ts</files>
  <read_first>milestory-frontend/package.json, milestory-frontend/src/app/app.ts, milestory-frontend/src/app/app.routes.ts, milestory-frontend/src/app/app.config.ts, milestory-frontend/src/app/app.spec.ts, .planning/phases/04-dashboard-and-motivational-ux/04-CONTEXT.md, .planning/phases/04-dashboard-and-motivational-ux/04-RESEARCH.md</read_first>
  <behavior>
    - Test 1: the root route redirects to `dashboard` inside `AppShellComponent`.
    - Test 2: the shell renders exact nav labels `Dashboard`, `Active goals`, `Archive`, and `New goal`.
    - Test 3: mobile drawer open/close state is keyboard accessible and returns focus to the menu trigger after close.
    - Test 4: `App` still renders a routed shell, but no longer depends on the placeholder home route.
  </behavior>
  <action>Per D-04 and PLAT-03, add `@angular/cdk` to `milestory-frontend/package.json` and implement `AppShellComponent` as the persistent root shell with a desktop sidebar plus mobile/tablet drawer behavior using CDK accessibility primitives instead of custom focus-trap code. Rewrite `app.routes.ts` so the root path loads `AppShellComponent`, redirects `''` to `'dashboard'`, keeps `dashboard`, `goals`, and archive/detail child navigation under the shell, and removes `HomePage` as the shipped root destination. Update `app.config.ts` to keep router provisioning compatible with the new shell, and update root tests so `App` still verifies the routed shell. The shell nav must show exact labels `Dashboard`, `Active goals`, `Archive`, and `New goal`, with `New goal` routing to `/goals/new` as the primary action.</action>
  <acceptance_criteria>
    - `rg -n "@angular/cdk" milestory-frontend/package.json` returns one match.
    - `rg -n "redirectTo: 'dashboard'" milestory-frontend/src/app/app.routes.ts` returns one match.
    - `rg -n "Dashboard|Active goals|Archive|New goal" milestory-frontend/src/app/shell/app-shell.component.html` returns four matches.
    - `rg -n "AppShellComponent" milestory-frontend/src/app/shell/app-shell.component.ts milestory-frontend/src/app/shell/app-shell.component.spec.ts` returns matches in both files.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix milestory-frontend test -- --watch=false --include src/app/shell/app-shell.component.spec.ts --include src/app/app.spec.ts</automated>
  </verify>
  <done>The app boots through a tested shell-first route tree, the placeholder home route is no longer the shipped default, and the shell navigation works across desktop and mobile interaction modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the dedicated active-goals page and wire shell navigation to existing goal routes</name>
  <files>milestory-frontend/src/app/features/goals/goal.routes.ts, milestory-frontend/src/app/features/goals/active/active-goals.page.ts, milestory-frontend/src/app/features/goals/active/active-goals.page.html, milestory-frontend/src/app/features/goals/active/active-goals.page.scss, milestory-frontend/src/app/features/goals/active/active-goals.page.spec.ts</files>
  <read_first>milestory-frontend/src/app/features/goals/goal.routes.ts, milestory-frontend/src/app/features/goals/archive/goal-archive.page.ts, milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts, milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.ts, milestory-frontend/src/app/features/dashboard/dashboard.page.ts</read_first>
  <behavior>
    - Test 1: `ActiveGoalsPage` loads `GoalPlanningStore.loadGoals('ACTIVE')` on construction.
    - Test 2: the page renders exact heading `Active goals` and reuses `app-goal-summary-card` for the list.
    - Test 3: the goal route tree exposes `/goals/active` without breaking `/goals/archive` or `:goalId`.
    - Test 4: active-goal cards navigate to `/goals/:goalId`.
  </behavior>
  <action>Create `ActiveGoalsPage` as a dedicated route at `/goals/active` per D-04, backed by `GoalPlanningStore.loadGoals('ACTIVE')` and reusing the shared `GoalSummaryCardComponent` from Plan 04-01. Update `goal.routes.ts` so `/goals/active` is available without letting the generic `:goalId` route capture it, preserving the earlier archive-before-detail routing safeguard. The page should present a slightly denser list than the dashboard while keeping the same status, `actual vs expected`, `next checkpoint`, and accomplishment-band signals. Wire the shell navigation so `Active goals` targets `/goals/active`, `Archive` targets `/goals/archive`, and `New goal` still targets `/goals/new`.</action>
  <acceptance_criteria>
    - `rg -n "path: 'active'" milestory-frontend/src/app/features/goals/goal.routes.ts` returns one match.
    - `rg -n "loadGoals\\('ACTIVE'\\)" milestory-frontend/src/app/features/goals/active/active-goals.page.ts` returns one match.
    - `rg -n "Active goals" milestory-frontend/src/app/features/goals/active/active-goals.page.html` returns one match.
    - `rg -n "app-goal-summary-card" milestory-frontend/src/app/features/goals/active/active-goals.page.html` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix milestory-frontend test -- --watch=false --include src/app/features/goals/active/active-goals.page.spec.ts --include src/app/shell/app-shell.component.spec.ts</automated>
  </verify>
  <done>A tested active-goals route exists, shell navigation routes to it cleanly, and detail/archive routes remain reachable without path conflicts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Align goal detail with the shared accomplishment-band treatment</name>
  <files>milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts, milestory-frontend/src/app/features/goals/detail/goal-detail.page.html, milestory-frontend/src/app/features/goals/detail/goal-detail.page.scss, milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts</files>
  <read_first>milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts, milestory-frontend/src/app/features/goals/detail/goal-detail.page.html, milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts, milestory-frontend/src/app/shared/ui/accomplishment-band/accomplishment-band.component.ts, milestory-frontend/src/app/features/dashboard/dashboard.page.html</read_first>
  <behavior>
    - Test 1: goal detail renders `app-accomplishment-band` in the status hero when the current progress reaches at least 80 percent.
    - Test 2: the badge copy matches the shared thresholds rather than detail-specific custom strings.
    - Test 3: existing progress overlay, history, and archived read-only behaviors stay green.
  </behavior>
  <action>Update `GoalDetailPage` to import and render the shared `AccomplishmentBandComponent` from Plan 04-01 inside the status hero so DASH-03 is consistent across dashboard and detail. Feed it directly from backend-owned `progressPercentOfTarget` and keep all existing pace copy, overlay workflow, history ordering, and archived-state protections intact. Do not move progress entry onto the dashboard; this task only aligns the celebration treatment and preserves the Phase 3 detail action surface.</action>
  <acceptance_criteria>
    - `rg -n "AccomplishmentBandComponent" milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts` returns one match.
    - `rg -n "app-accomplishment-band" milestory-frontend/src/app/features/goals/detail/goal-detail.page.html` returns one match.
    - `rg -n "80% milestone|Target reached|120% stretch" milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts` returns at least one match tied to shared-band assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix milestory-frontend test -- --watch=false --include src/app/features/goals/detail/goal-detail.page.spec.ts --include src/app/shared/ui/accomplishment-band/accomplishment-band.component.spec.ts</automated>
  </verify>
  <done>Goal detail reuses the exact shared accomplishment-band treatment, and the existing Phase 3 progress/detail behaviors remain covered by tests.</done>
</task>

</tasks>

<verification>
After the targeted task-level specs, run `npm --prefix milestory-frontend test -- --watch=false` as the Wave 2 gate because this plan rewires the root shell and existing goal routes.
</verification>

<success_criteria>
The shipped app now opens into a Milestory shell with dashboard-first navigation, a dedicated active-goals route, clear drill-downs to goal detail, and shared celebratory accomplishment rendering across summary and detail surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-and-motivational-ux/04-02-SUMMARY.md`
</output>
