---
phase: 04-dashboard-and-motivational-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - milestory-frontend/package.json
  - milestory-frontend/src/app/app.config.ts
  - milestory-frontend/src/app/app.routes.ts
  - milestory-frontend/src/app/app.html
  - milestory-frontend/src/app/app.spec.ts
  - milestory-frontend/src/app/shell/app-shell.component.ts
  - milestory-frontend/src/app/shell/app-shell.component.html
  - milestory-frontend/src/app/shell/app-shell.component.scss
  - milestory-frontend/src/app/shell/app-shell.component.spec.ts
  - milestory-frontend/src/app/features/goals/goal.routes.ts
  - milestory-frontend/src/app/features/goals/active/active-goals.page.ts
  - milestory-frontend/src/app/features/goals/active/active-goals.page.html
  - milestory-frontend/src/app/features/goals/active/active-goals.page.scss
  - milestory-frontend/src/app/features/goals/active/active-goals.page.spec.ts
autonomous: true
requirements:
  - PLAT-03
must_haves:
  truths:
    - "User lands inside a real Milestory shell instead of the starter home placeholder."
    - "User can move between Dashboard, Active goals, Archive, and New goal from a persistent sidebar or mobile drawer."
    - "Dashboard becomes the default shipped entry route inside the shell."
    - "The `Active goals` shell destination is reserved by an explicit route so it cannot be captured by `:goalId`."
  artifacts:
    - path: "milestory-frontend/src/app/shell/app-shell.component.ts"
      provides: "Persistent app shell with sidebar and mobile drawer"
      contains: "export class AppShellComponent"
    - path: "milestory-frontend/src/app/app.routes.ts"
      provides: "Shell-first route tree with dashboard as default entry"
      contains: "redirectTo: 'dashboard'"
    - path: "milestory-frontend/src/app/features/goals/goal.routes.ts"
      provides: "Goal route tree with `active` reserved ahead of `:goalId`"
      contains: "path: 'active'"
  key_links:
    - from: "milestory-frontend/src/app/app.routes.ts"
      to: "milestory-frontend/src/app/shell/app-shell.component.ts"
      via: "root shell route"
      pattern: "loadComponent.*app-shell"
    - from: "milestory-frontend/src/app/shell/app-shell.component.html"
      to: "milestory-frontend/src/app/app.routes.ts"
      via: "dashboard nav link and child outlet"
      pattern: "routerLink=.*dashboard"
    - from: "milestory-frontend/src/app/shell/app-shell.component.html"
      to: "milestory-frontend/src/app/features/goals/goal.routes.ts"
      via: "Active goals nav target is backed by an explicit route entry"
      pattern: "routerLink=.*goals/active|path: 'active'"
---

<objective>
Replace the placeholder root experience with the real Milestory shell and dashboard-first route tree.

Purpose: Satisfy PLAT-03 by establishing the persistent shell, dashboard-first entry flow, and responsive navigation foundation before downstream route content work.
Output: A tested shell with sidebar/drawer navigation and dashboard as the default entry route.
</objective>

<execution_context>
@/Users/ybritto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ybritto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-and-motivational-ux/04-CONTEXT.md
@.planning/phases/04-dashboard-and-motivational-ux/04-RESEARCH.md
@.planning/phases/04-dashboard-and-motivational-ux/04-VALIDATION.md
@.planning/phases/04-dashboard-and-motivational-ux/04-01-PLAN.md
@milestory-frontend/src/app/app.routes.ts
@milestory-frontend/src/app/app.html
@milestory-frontend/src/app/app.config.ts
@milestory-frontend/src/app/features/goals/goal.routes.ts
@milestory-frontend/src/app/features/dashboard/dashboard.page.ts

<interfaces>
From `milestory-frontend/src/app/app.routes.ts`:
```typescript
export const routes: Routes = [
  {
    path: '',
    loadComponent: () =>
      import('./features/home/home.page').then((module) => module.HomePage),
  },
  {
    path: 'goals',
    loadChildren: () =>
      import('./features/goals/goal.routes').then((module) => module.GOAL_ROUTES),
  },
];
```

From `milestory-frontend/src/app/features/dashboard/dashboard.page.ts` after Plan 04-01:
```typescript
export class DashboardPage {}
```

Locked decisions to implement:
- Per D-04, the shell must expose `Dashboard`, `Active goals`, `Archive`, and a strong `New goal` action.
- Per D-04, the sidebar stays visible on desktop and collapses into a drawer on mobile/tablet.
- Per D-04, the old home page stops being the primary shipped destination once the shell exists.
- Deferred: no dashboard-side progress logging or checkpoint editing, and no medium-density active-goals content work in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the shell-first route tree and responsive app shell</name>
  <files>milestory-frontend/package.json, milestory-frontend/src/app/app.config.ts, milestory-frontend/src/app/app.routes.ts, milestory-frontend/src/app/app.html, milestory-frontend/src/app/app.spec.ts, milestory-frontend/src/app/shell/app-shell.component.ts, milestory-frontend/src/app/shell/app-shell.component.html, milestory-frontend/src/app/shell/app-shell.component.scss, milestory-frontend/src/app/shell/app-shell.component.spec.ts, milestory-frontend/src/app/features/goals/goal.routes.ts, milestory-frontend/src/app/features/goals/active/active-goals.page.ts, milestory-frontend/src/app/features/goals/active/active-goals.page.html, milestory-frontend/src/app/features/goals/active/active-goals.page.scss, milestory-frontend/src/app/features/goals/active/active-goals.page.spec.ts</files>
  <read_first>milestory-frontend/package.json, milestory-frontend/src/app/app.ts, milestory-frontend/src/app/app.routes.ts, milestory-frontend/src/app/app.config.ts, milestory-frontend/src/app/app.spec.ts, milestory-frontend/src/app/features/goals/goal.routes.ts, milestory-frontend/src/app/features/dashboard/dashboard.page.ts, .planning/phases/04-dashboard-and-motivational-ux/04-CONTEXT.md, .planning/phases/04-dashboard-and-motivational-ux/04-RESEARCH.md</read_first>
  <behavior>
    - Test 1: the root route redirects to `dashboard` inside `AppShellComponent`.
    - Test 2: the shell renders exact nav labels `Dashboard`, `Active goals`, `Archive`, and `New goal`.
    - Test 3: mobile drawer open/close state is keyboard accessible and returns focus to the menu trigger after close.
    - Test 4: `/goals/active` resolves to a reserved route entry and is not captured by `:goalId`.
    - Test 5: `App` still renders a routed shell, but no longer depends on the placeholder home route.
  </behavior>
  <action>Per D-04 and PLAT-03, add `@angular/cdk` to `milestory-frontend/package.json` and implement `AppShellComponent` as the persistent root shell with a desktop sidebar plus mobile/tablet drawer behavior using CDK accessibility primitives instead of custom focus-trap code. Rewrite `app.routes.ts` so the root path loads `AppShellComponent`, redirects `''` to `'dashboard'`, keeps `dashboard` and `goals` under the shell, and removes `HomePage` as the shipped root destination. Reserve `/goals/active` in `goal.routes.ts` before the generic `:goalId` route, and add a lightweight `ActiveGoalsPage` placeholder component whose only job in this plan is to make the shell destination real and testable without yet implementing the medium-density shared-card list from Plan 04-03. Include shell nav items with exact labels `Dashboard`, `Active goals`, `Archive`, and `New goal`, routing them to `/dashboard`, `/goals/active`, `/goals/archive`, and `/goals/new`. Update `app.config.ts` to keep router provisioning compatible with the new shell, and update root tests so `App` still verifies the routed shell and `/goals/active` is not captured as a detail route.</action>
  <acceptance_criteria>
    - `rg -n "@angular/cdk" milestory-frontend/package.json` returns one match.
    - `rg -n "redirectTo: 'dashboard'" milestory-frontend/src/app/app.routes.ts` returns one match.
    - `rg -n "Dashboard|Active goals|Archive|New goal" milestory-frontend/src/app/shell/app-shell.component.html` returns four matches.
    - `rg -n "path: 'active'" milestory-frontend/src/app/features/goals/goal.routes.ts` returns one match before the `:goalId` route.
    - `rg -n "Active goals" milestory-frontend/src/app/features/goals/active/active-goals.page.html` returns one match.
    - `rg -n "AppShellComponent" milestory-frontend/src/app/shell/app-shell.component.ts milestory-frontend/src/app/shell/app-shell.component.spec.ts` returns matches in both files.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix milestory-frontend test -- --watch=false --include src/app/shell/app-shell.component.spec.ts --include src/app/app.spec.ts --include src/app/features/goals/active/active-goals.page.spec.ts</automated>
  </verify>
  <done>The app boots through a tested shell-first route tree, the placeholder home route is no longer the shipped default, shell navigation works across desktop and mobile interaction modes, and `/goals/active` is safely reserved for Plan 04-03.</done>
</task>

</tasks>

<verification>
Run `npm --prefix milestory-frontend test -- --watch=false --include src/app/shell/app-shell.component.spec.ts --include src/app/app.spec.ts --include src/app/features/goals/active/active-goals.page.spec.ts` after the task, then run the full frontend suite once before handing off to Plan 04-03 because this plan rewires the root shell, entry route, and reserves the active-goals path.
</verification>

<success_criteria>
The shipped app now opens into a Milestory shell with dashboard-first navigation and responsive shell behavior, while downstream active-goals and detail-band work remain isolated for the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-and-motivational-ux/04-02-SUMMARY.md`
</output>
