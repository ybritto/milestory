---
phase: 04-dashboard-and-motivational-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - milestory-frontend/src/app/features/dashboard/shared/dashboard-view.models.ts
  - milestory-frontend/src/app/features/dashboard/shared/dashboard.presenter.ts
  - milestory-frontend/src/app/features/dashboard/shared/dashboard.presenter.spec.ts
  - milestory-frontend/src/app/shared/ui/accomplishment-band/accomplishment-band.component.ts
  - milestory-frontend/src/app/shared/ui/accomplishment-band/accomplishment-band.component.spec.ts
  - milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.ts
  - milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.html
  - milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.scss
  - milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.spec.ts
  - milestory-frontend/src/app/features/dashboard/dashboard.page.ts
  - milestory-frontend/src/app/features/dashboard/dashboard.page.html
  - milestory-frontend/src/app/features/dashboard/dashboard.page.scss
  - milestory-frontend/src/app/features/dashboard/dashboard.page.spec.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "User can see a dashboard command center with counts, strongest risk, strongest win, and one suggested next goal."
    - "User can scan active goals in Behind, On pace, and Ahead groups ordered by urgency."
    - "User can see bold accomplishment bands at 80%, 100%, and 120% without client-side status recomputation."
    - "User can open goal detail from dashboard callouts and grouped goal cards."
  artifacts:
    - path: "milestory-frontend/src/app/features/dashboard/dashboard.page.ts"
      provides: "Standalone dashboard page backed by active goal list data"
      contains: "export class DashboardPage"
    - path: "milestory-frontend/src/app/features/dashboard/shared/dashboard.presenter.ts"
      provides: "Pure dashboard grouping, urgency ordering, and callout derivation"
      contains: "buildDashboardViewModel"
    - path: "milestory-frontend/src/app/shared/ui/accomplishment-band/accomplishment-band.component.ts"
      provides: "Shared celebratory accomplishment-band rendering"
      contains: "AccomplishmentBandComponent"
    - path: "milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.ts"
      provides: "Reusable medium-density goal summary card with drill-down link"
      contains: "GoalSummaryCardComponent"
  key_links:
    - from: "milestory-frontend/src/app/features/dashboard/dashboard.page.ts"
      to: "milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts"
      via: "loadGoals('ACTIVE')"
      pattern: "loadGoals\\('ACTIVE'\\)"
    - from: "milestory-frontend/src/app/features/dashboard/dashboard.page.html"
      to: "/goals/:goalId"
      via: "command-center strongest risk, strongest win, and suggested next CTA drill-down"
      pattern: "Open next|routerLink"
    - from: "milestory-frontend/src/app/features/dashboard/dashboard.page.html"
      to: "milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.ts"
      via: "goal-summary-card component usage"
      pattern: "app-goal-summary-card"
    - from: "milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.html"
      to: "/goals/:goalId"
      via: "routerLink drill-down"
      pattern: "routerLink"
---

<objective>
Build the dashboard’s shared presentation layer and the dashboard page itself on top of the Phase 3 active-goal payload.

Purpose: Implement the motivating command-center experience per D-04 dashboard decisions without adding frontend-owned progress math or deferred edit actions.
Output: A pure presenter for grouping/urgency, a shared accomplishment band, a reusable goal summary card, and a dashboard page with automated tests.
</objective>

<execution_context>
@/Users/ybritto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ybritto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-and-motivational-ux/04-CONTEXT.md
@.planning/phases/04-dashboard-and-motivational-ux/04-RESEARCH.md
@.planning/phases/04-dashboard-and-motivational-ux/04-VALIDATION.md
@.planning/phases/03-progress-engine-and-status/03-02-SUMMARY.md
@.planning/phases/03-progress-engine-and-status/03-03-SUMMARY.md
@milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts
@milestory-frontend/src/api/model/listGoals200ResponseInner.ts
@milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts
@milestory-frontend/src/styles.scss

<interfaces>
From `milestory-frontend/src/api/model/listGoals200ResponseInner.ts`:
```typescript
export interface ListGoals200ResponseInner {
  goalId: string;
  title: string;
  targetValue: number;
  unit: string;
  currentProgressValue: number;
  progressPercentOfTarget: number;
  expectedProgressValueToday: number;
  paceStatus: 'BEHIND' | 'ON_PACE' | 'AHEAD';
  paceSummary: string;
  paceDetail: string;
  progressEntries: Array<ListGoals200ResponseInnerProgressEntriesInner>;
  checkpoints: Array<PreviewGoalPlan200ResponseCheckpointsInner>;
}
```

From `milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts`:
```typescript
export class GoalPlanningStore {
  readonly goals = this.goalsState.asReadonly();
  loadGoals(status?: 'ACTIVE' | 'ARCHIVED'): void;
}
```

Locked decisions to implement:
- Per D-04, command center must show counts, strongest risk, strongest win, and a suggested next goal.
- Per D-04, group cards into `Behind`, `On pace`, and `Ahead`, with the most urgent items first.
- Per D-04, accomplishment bands must be bold and milestone-based at meaningful thresholds.
- Deferred: do not add direct progress logging or checkpoint editing on the dashboard.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define dashboard presentation contracts and failing tests</name>
  <files>milestory-frontend/src/app/features/dashboard/shared/dashboard-view.models.ts, milestory-frontend/src/app/features/dashboard/shared/dashboard.presenter.ts, milestory-frontend/src/app/features/dashboard/shared/dashboard.presenter.spec.ts</files>
  <read_first>milestory-frontend/src/api/model/listGoals200ResponseInner.ts, milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts, milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts, .planning/phases/04-dashboard-and-motivational-ux/04-CONTEXT.md, .planning/phases/04-dashboard-and-motivational-ux/04-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `buildDashboardViewModel` groups goals into `behind`, `onPace`, and `ahead` arrays from backend `paceStatus` values and orders each group by urgency.
    - Test 2: `buildDashboardViewModel` emits `strongestRiskGoalId`, `strongestWinGoalId`, `suggestedNextGoalId`, and `activeGoalCount` from the same active-goal input.
  </behavior>
  <action>Per D-04 and DASH-01/02/04, create a pure presenter contract file that defines `DashboardViewModel`, `DashboardGroupKey`, `DashboardCallout`, and any goal-card input type needed by both dashboard and later active-goals work. Write failing specs first for grouping, urgency ordering, strongest risk/win derivation, and suggested-next-goal selection using only the presenter/model seam created in this task. Use concrete fixture goals with `paceStatus` values `BEHIND`, `ON_PACE`, and `AHEAD`, with descending urgency driven only by backend-owned fields such as `progressPercentOfTarget`, `expectedProgressValueToday`, `currentProgressValue`, and checkpoint dates already present in the payload. Do not invent new domain statuses or any dashboard-side progress mutation actions. Leave `AccomplishmentBandComponent` and `DashboardPage` specs for the later tasks that create those implementation files.</action>
  <acceptance_criteria>
    - `rg -n "buildDashboardViewModel|DashboardViewModel|DashboardGroupKey" milestory-frontend/src/app/features/dashboard/shared/dashboard-view.models.ts milestory-frontend/src/app/features/dashboard/shared/dashboard.presenter.ts` returns matches.
    - `rg -n "strongestRiskGoalId|strongestWinGoalId|suggestedNextGoalId|activeGoalCount" milestory-frontend/src/app/features/dashboard/shared/dashboard.presenter.spec.ts` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix milestory-frontend test -- --watch=false --include src/app/features/dashboard/shared/dashboard.presenter.spec.ts</automated>
  </verify>
  <done>Dashboard contracts exist, the presenter specs encode grouping and callout behavior, and the task-level test command runs against files that exist within this task’s scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure presenter and shared accomplishment-card UI</name>
  <files>milestory-frontend/src/app/features/dashboard/shared/dashboard.presenter.ts, milestory-frontend/src/app/features/dashboard/shared/dashboard.presenter.spec.ts, milestory-frontend/src/app/shared/ui/accomplishment-band/accomplishment-band.component.ts, milestory-frontend/src/app/shared/ui/accomplishment-band/accomplishment-band.component.spec.ts, milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.ts, milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.html, milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.scss, milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.spec.ts</files>
  <read_first>milestory-frontend/src/app/features/dashboard/shared/dashboard-view.models.ts, milestory-frontend/src/app/features/dashboard/shared/dashboard.presenter.spec.ts, milestory-frontend/src/api/model/listGoals200ResponseInner.ts, milestory-frontend/src/styles.scss, milestory-frontend/src/app/features/goals/detail/goal-detail.page.html</read_first>
  <behavior>
    - Test 1: presenter output keeps `BEHIND` goals first and sorts the group so the lowest plan attainment appears before less urgent peers.
    - Test 2: `GoalSummaryCardComponent` shows exact copy labels `Actual so far`, `Expected by today`, and `Next checkpoint`.
    - Test 3: `GoalSummaryCardComponent` renders a router-linked container or CTA targeting `/goals/<goalId>`.
    - Test 4: `AccomplishmentBandComponent` applies a celebratory variant for progress at or above 120 percent.
  </behavior>
  <action>Implement `buildDashboardViewModel` as a pure function that accepts `ListGoals200ResponseInner[]` and returns grouped sections, counts, and command-center callouts using backend fields only. Treat urgency as a presentation sort, not new domain logic: order `BEHIND` by largest shortfall first, `ON_PACE` by closest upcoming checkpoint or lowest percent first, and `AHEAD` by highest accomplishment first. Create `AccomplishmentBandComponent` as a standalone shared UI component with exact label mapping `80% milestone`, `Target reached`, and `120% stretch`; make it visually bold per D-04 and return no badge before 80 percent. Create `GoalSummaryCardComponent` as a standalone medium-density card that shows concise status context, `actual vs expected`, `next checkpoint`, and the strongest risk or win signal, plus a drill-down router link. Keep all celebration and tone presentation in these shared UI pieces so the dashboard and later goal detail use one source.</action>
  <acceptance_criteria>
    - `rg -n "Actual so far|Expected by today|Next checkpoint" milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.html` returns three matches.
    - `rg -n "80% milestone|Target reached|120% stretch" milestory-frontend/src/app/shared/ui/accomplishment-band/accomplishment-band.component.ts` returns three matches.
    - `rg -n "routerLink" milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.html` returns at least one match.
    - `rg -n "buildDashboardViewModel" milestory-frontend/src/app/features/dashboard/shared/dashboard.presenter.ts` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix milestory-frontend test -- --watch=false --include src/app/features/dashboard/shared/dashboard.presenter.spec.ts --include src/app/shared/ui/accomplishment-band/accomplishment-band.component.spec.ts --include src/app/shared/ui/goal-summary-card/goal-summary-card.component.spec.ts</automated>
  </verify>
  <done>The presenter derives all dashboard sections and callouts from active-goal data, the shared goal card exposes the required summary fields and drill-down link, and accomplishment thresholds are centralized in one tested component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the dashboard page command center and grouped sections</name>
  <files>milestory-frontend/src/app/features/dashboard/dashboard.page.ts, milestory-frontend/src/app/features/dashboard/dashboard.page.html, milestory-frontend/src/app/features/dashboard/dashboard.page.scss, milestory-frontend/src/app/features/dashboard/dashboard.page.spec.ts</files>
  <read_first>milestory-frontend/src/app/features/dashboard/shared/dashboard.presenter.ts, milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.ts, milestory-frontend/src/app/shared/ui/accomplishment-band/accomplishment-band.component.ts, milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts, milestory-frontend/src/app/features/home/home.page.scss</read_first>
  <behavior>
    - Test 1: page construction calls `loadGoals('ACTIVE')` and reads state from `GoalPlanningStore.goals`.
    - Test 2: the page renders a command center with exact labels `Active goals`, `Strongest risk`, `Strongest win`, and `Open next`.
    - Test 3: the page renders grouped sections only when that group has items and preserves the exact section headings `Behind`, `On pace`, and `Ahead`.
    - Test 4: strongest risk/win callouts and goal cards each navigate to `/goals/:goalId` instead of opening an inline edit or progress form.
  </behavior>
  <action>Implement `DashboardPage` as a standalone page that loads active goals from `GoalPlanningStore.loadGoals('ACTIVE')`, passes `GoalPlanningStore.goals()` through `buildDashboardViewModel`, and renders the practical command-center summary required by D-04. The command center must show active-goal count, strongest risk, strongest win, and one suggested next goal with exact CTA label `Open next`. Below that, render `Behind`, `On pace`, and `Ahead` sections using `GoalSummaryCardComponent`, preserving medium card density and routing each card to `/goals/:goalId`. Use the warm Milestory visual language from existing SCSS, but keep the dashboard as a decision surface rather than a mini detail page stack. Do not add archive actions, direct progress logging, or checkpoint editing controls here.</action>
  <acceptance_criteria>
    - `rg -n "loadGoals\\('ACTIVE'\\)" milestory-frontend/src/app/features/dashboard/dashboard.page.ts` returns one match.
    - `rg -n "Active goals|Strongest risk|Strongest win|Open next" milestory-frontend/src/app/features/dashboard/dashboard.page.html` returns four matches.
    - `rg -n ">Behind<|>On pace<|>Ahead<" milestory-frontend/src/app/features/dashboard/dashboard.page.html` returns matches for all three headings.
    - `rg -n "Open next|routerLink" milestory-frontend/src/app/features/dashboard/dashboard.page.html` returns matches proving command-center drill-down wiring exists.
    - `rg -n "app-goal-summary-card" milestory-frontend/src/app/features/dashboard/dashboard.page.html` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix milestory-frontend test -- --watch=false --include src/app/features/dashboard/dashboard.page.spec.ts --include src/app/features/dashboard/shared/dashboard.presenter.spec.ts --include src/app/shared/ui/goal-summary-card/goal-summary-card.component.spec.ts --include src/app/shared/ui/accomplishment-band/accomplishment-band.component.spec.ts</automated>
  </verify>
  <done>The dashboard page is implemented and tested as the Phase 4 summary surface, showing command-center callouts, grouped cards, accomplishment cues, and drill-down links without introducing new dashboard-side domain logic.</done>
</task>

</tasks>

<verification>
Run the targeted Phase 4 dashboard specs after each task, then run the full frontend suite once before handing off to Wave 2.
</verification>

<success_criteria>
The repository contains a tested dashboard page and reusable summary/accomplishment UI that read backend-owned goal status fields directly and expose obvious navigation into goal detail.
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-and-motivational-ux/04-01-SUMMARY.md`
</output>
