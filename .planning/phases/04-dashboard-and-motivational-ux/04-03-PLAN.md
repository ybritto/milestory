---
phase: 04-dashboard-and-motivational-ux
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - milestory-frontend/src/app/features/goals/goal.routes.ts
  - milestory-frontend/src/app/features/goals/active/active-goals.page.ts
  - milestory-frontend/src/app/features/goals/active/active-goals.page.html
  - milestory-frontend/src/app/features/goals/active/active-goals.page.scss
  - milestory-frontend/src/app/features/goals/active/active-goals.page.spec.ts
  - milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts
  - milestory-frontend/src/app/features/goals/detail/goal-detail.page.html
  - milestory-frontend/src/app/features/goals/detail/goal-detail.page.scss
  - milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts
autonomous: true
requirements:
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "User can scan all active goals on a dedicated route and drill into goal detail without route confusion."
    - "User sees the same accomplishment-band treatment on goal detail that appears on the dashboard."
    - "The `Active goals` shell destination resolves to real content rather than a dead route."
  artifacts:
    - path: "milestory-frontend/src/app/features/goals/active/active-goals.page.ts"
      provides: "Dedicated active-goals route"
      contains: "export class ActiveGoalsPage"
    - path: "milestory-frontend/src/app/features/goals/goal.routes.ts"
      provides: "Goal route tree with explicit active-goals routing ahead of :goalId"
      contains: "path: 'active'"
    - path: "milestory-frontend/src/app/features/goals/detail/goal-detail.page.html"
      provides: "Goal detail accomplishment-band rendering"
      contains: "app-accomplishment-band"
  key_links:
    - from: "milestory-frontend/src/app/features/goals/goal.routes.ts"
      to: "milestory-frontend/src/app/features/goals/active/active-goals.page.ts"
      via: "active-goals route registration"
      pattern: "path: 'active'"
    - from: "milestory-frontend/src/app/features/goals/active/active-goals.page.html"
      to: "milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.ts"
      via: "shared goal summary cards"
      pattern: "app-goal-summary-card"
    - from: "milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts"
      to: "milestory-frontend/src/app/shared/ui/accomplishment-band/accomplishment-band.component.ts"
      via: "shared accomplishment-band import"
      pattern: "AccomplishmentBandComponent"
---

<objective>
Add the dedicated active-goals route and align goal detail with the shared accomplishment-band treatment.

Purpose: Complete the dashboard drill-down loop and unify celebration treatment across summary and detail surfaces without overloading the shell plan.
Output: A tested active-goals page, goal-route wiring that avoids `:goalId` collisions, and aligned accomplishment rendering in goal detail.
</objective>

<execution_context>
@/Users/ybritto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ybritto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-and-motivational-ux/04-CONTEXT.md
@.planning/phases/04-dashboard-and-motivational-ux/04-RESEARCH.md
@.planning/phases/04-dashboard-and-motivational-ux/04-VALIDATION.md
@.planning/phases/04-dashboard-and-motivational-ux/04-01-PLAN.md
@.planning/phases/04-dashboard-and-motivational-ux/04-02-PLAN.md
@milestory-frontend/src/app/features/goals/goal.routes.ts
@milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts
@milestory-frontend/src/app/features/goals/archive/goal-archive.page.ts
@milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.ts
@milestory-frontend/src/app/shared/ui/accomplishment-band/accomplishment-band.component.ts

<interfaces>
From `milestory-frontend/src/app/features/goals/goal.routes.ts` before this plan:
```typescript
export const GOAL_ROUTES: Routes = [
  { path: '', pathMatch: 'full', redirectTo: 'new' },
  { path: 'active', ... },
  { path: 'new', ... },
  { path: 'archive', ... },
  { path: ':goalId', ... },
];
```

From `milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.ts` after Plan 04-01:
```typescript
export class GoalSummaryCardComponent {}
```

Locked decisions to implement:
- Per D-04, `Active goals` must be a real shell destination.
- Per D-04, dashboard and active-goals drill-downs must route into goal detail rather than inline editing.
- Per D-04, accomplishment bands must stay bold and consistent across dashboard and detail.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the dedicated active-goals page and route wiring</name>
  <files>milestory-frontend/src/app/features/goals/goal.routes.ts, milestory-frontend/src/app/features/goals/active/active-goals.page.ts, milestory-frontend/src/app/features/goals/active/active-goals.page.html, milestory-frontend/src/app/features/goals/active/active-goals.page.scss, milestory-frontend/src/app/features/goals/active/active-goals.page.spec.ts</files>
  <read_first>milestory-frontend/src/app/features/goals/goal.routes.ts, milestory-frontend/src/app/features/goals/archive/goal-archive.page.ts, milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts, milestory-frontend/src/app/shared/ui/goal-summary-card/goal-summary-card.component.ts, milestory-frontend/src/app/features/dashboard/dashboard.page.ts, .planning/phases/04-dashboard-and-motivational-ux/04-02-PLAN.md</read_first>
  <behavior>
    - Test 1: `ActiveGoalsPage` calls `GoalPlanningStore.loadGoals('ACTIVE')` on construction.
    - Test 2: the page renders exact heading `Active goals` and reuses `app-goal-summary-card` for the list.
    - Test 3: the goal route tree exposes `/goals/active` without letting `:goalId` capture it, while preserving `/goals/archive`.
    - Test 4: active-goal cards navigate to `/goals/:goalId`.
  </behavior>
  <action>Expand the reserved `ActiveGoalsPage` from Plan 04-02 into the real `/goals/active` destination per D-04, backed by `GoalPlanningStore.loadGoals('ACTIVE')` and reusing the shared `GoalSummaryCardComponent` from Plan 04-01. Keep `path: 'active'` ahead of the generic `:goalId` route in `goal.routes.ts`, preserving the earlier archive-before-detail routing safeguard. The page should present a slightly denser list than the dashboard while keeping the same status, `Actual so far`, `Expected by today`, `Next checkpoint`, and accomplishment-band signals from the shared summary card. Keep drill-down behavior router-based only; do not add inline edit or progress controls.</action>
  <acceptance_criteria>
    - `rg -n "path: 'active'" milestory-frontend/src/app/features/goals/goal.routes.ts` returns one match.
    - `rg -n "loadGoals\\('ACTIVE'\\)" milestory-frontend/src/app/features/goals/active/active-goals.page.ts` returns one match.
    - `rg -n "Active goals" milestory-frontend/src/app/features/goals/active/active-goals.page.html` returns one match.
    - `rg -n "app-goal-summary-card" milestory-frontend/src/app/features/goals/active/active-goals.page.html` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix milestory-frontend test -- --watch=false --include src/app/features/goals/active/active-goals.page.spec.ts</automated>
  </verify>
  <done>A tested active-goals route exists, shell navigation resolves to it cleanly, and detail/archive routes remain reachable without path conflicts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align goal detail with the shared accomplishment-band treatment</name>
  <files>milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts, milestory-frontend/src/app/features/goals/detail/goal-detail.page.html, milestory-frontend/src/app/features/goals/detail/goal-detail.page.scss, milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts</files>
  <read_first>milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts, milestory-frontend/src/app/features/goals/detail/goal-detail.page.html, milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts, milestory-frontend/src/app/shared/ui/accomplishment-band/accomplishment-band.component.ts, milestory-frontend/src/app/features/dashboard/dashboard.page.html</read_first>
  <behavior>
    - Test 1: goal detail renders `app-accomplishment-band` in the status hero when the current progress reaches at least 80 percent.
    - Test 2: the badge copy matches the shared thresholds rather than detail-specific custom strings.
    - Test 3: existing progress overlay, history, and archived read-only behaviors stay green.
  </behavior>
  <action>Update `GoalDetailPage` to import and render the shared `AccomplishmentBandComponent` from Plan 04-01 inside the status hero so DASH-03 is consistent across dashboard and detail. Feed it directly from backend-owned `progressPercentOfTarget` and keep all existing pace copy, overlay workflow, history ordering, and archived-state protections intact. Do not move progress entry onto the dashboard; this task only aligns the celebration treatment and preserves the Phase 3 detail action surface.</action>
  <acceptance_criteria>
    - `rg -n "AccomplishmentBandComponent" milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts` returns one match.
    - `rg -n "app-accomplishment-band" milestory-frontend/src/app/features/goals/detail/goal-detail.page.html` returns one match.
    - `rg -n "80% milestone|Target reached|120% stretch" milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts` returns at least one match tied to shared-band assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix milestory-frontend test -- --watch=false --include src/app/features/goals/detail/goal-detail.page.spec.ts --include src/app/shared/ui/accomplishment-band/accomplishment-band.component.spec.ts</automated>
  </verify>
  <done>Goal detail reuses the exact shared accomplishment-band treatment, and the existing Phase 3 progress/detail behaviors remain covered by tests.</done>
</task>

</tasks>

<verification>
Run the targeted active-goals and goal-detail specs after each task, then run `npm --prefix milestory-frontend test -- --watch=false` as the Wave 3 gate because this plan finishes the shell destinations and touches an existing detail route.
</verification>

<success_criteria>
The app now has a real Active goals destination, drill-downs from summary surfaces land in goal detail without path conflicts, and accomplishment bands render consistently across dashboard and detail.
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-and-motivational-ux/04-03-SUMMARY.md`
</output>
