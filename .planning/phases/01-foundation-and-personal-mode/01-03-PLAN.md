---
phase: 01-foundation-and-personal-mode
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - milestory-frontend/src/app/app.config.ts
  - milestory-frontend/src/app/app.routes.ts
  - milestory-frontend/src/app/app.ts
  - milestory-frontend/src/app/app.html
  - milestory-frontend/src/app/app.scss
  - milestory-frontend/src/app/app.spec.ts
  - milestory-frontend/src/styles.scss
  - milestory-frontend/src/app/features/home/home.page.ts
  - milestory-frontend/src/app/features/home/home.page.html
  - milestory-frontend/src/app/features/home/home.page.scss
  - milestory-frontend/src/app/features/home/home.spec.ts
  - milestory-frontend/src/app/core/status/foundation-status.store.ts
  - milestory-frontend/src/app/core/status/foundation-status.store.spec.ts
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
must_haves:
  truths:
    - "User sees a Milestory-branded home screen instead of Angular starter content."
    - "The home screen loads backend status immediately and renders ready, empty, loading, or error states."
    - "The frontend displays backend-owned status facts without any auth UI or bearer token handling."
  artifacts:
    - path: "milestory-frontend/src/app/features/home/home.page.ts"
      provides: "Home page component for the Phase 1 route"
      contains: "Refresh status"
    - path: "milestory-frontend/src/app/core/status/foundation-status.store.ts"
      provides: "Signal-based status loading and retry logic"
      contains: "loadStatus"
    - path: "milestory-frontend/src/styles.scss"
      provides: "Phase 1 design tokens and font imports"
      contains: "Fraunces"
    - path: "milestory-frontend/src/app/app.spec.ts"
      provides: "Root shell coverage without starter title assertions"
      contains: "Milestory"
  key_links:
    - from: "milestory-frontend/src/app/app.config.ts"
      to: "milestory-frontend/src/api/provide-api.ts"
      via: "provideHttpClient and provideApi"
      pattern: "provideApi"
    - from: "milestory-frontend/src/app/core/status/foundation-status.store.ts"
      to: "milestory-frontend/src/api/api/default.service.ts"
      via: "generated client call"
      pattern: "apiV1StatusGet"
    - from: "milestory-frontend/src/app/features/home/home.page.ts"
      to: "milestory-frontend/src/app/core/status/foundation-status.store.ts"
      via: "signals-based view model"
      pattern: "FoundationStatusStore"
---

<objective>
Replace the Angular starter shell with the polished Milestory Phase 1 home screen that renders backend-owned foundation status through the generated auth-free client.

Purpose: Implement D-05 through D-08 and D-11 while keeping PLAT-02 intact by making the frontend a thin renderer over backend data.
Output: A single `/` route with the three-zone home layout from the UI spec, signal-based status loading and retry, and tests proving the UI shows Milestory instead of starter/auth scaffolding.
</objective>

<execution_context>
@/Users/ybritto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ybritto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-personal-mode/01-CONTEXT.md
@.planning/phases/01-foundation-and-personal-mode/01-UI-SPEC.md
@.planning/phases/01-foundation-and-personal-mode/01-VALIDATION.md
@.planning/phases/01-foundation-and-personal-mode/01-01-PLAN.md
@.planning/phases/01-foundation-and-personal-mode/01-02-PLAN.md
@milestory-frontend/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the frontend app shell to the auth-free generated status client</name>
  <read_first>
    milestory-frontend/src/app/app.config.ts
    milestory-frontend/src/app/app.routes.ts
    milestory-frontend/src/app/app.ts
    milestory-frontend/src/api/provide-api.ts
    milestory-frontend/src/api/api/default.service.ts
    .planning/phases/01-foundation-and-personal-mode/01-UI-SPEC.md
  </read_first>
  <files>milestory-frontend/src/app/app.config.ts, milestory-frontend/src/app/app.routes.ts, milestory-frontend/src/app/app.ts, milestory-frontend/src/app/core/status/foundation-status.store.ts, milestory-frontend/src/app/core/status/foundation-status.store.spec.ts</files>
  <acceptance_criteria>
    - `rg -n "provideHttpClient|provideApi" milestory-frontend/src/app/app.config.ts` returns matches.
    - `rg -n "path: ''|loadComponent|HomePage" milestory-frontend/src/app/app.routes.ts` returns matches.
    - `rg -n "DefaultService|signal|loadStatus|refreshStatus" milestory-frontend/src/app/core/status/foundation-status.store.ts` returns matches.
    - `rg -n "Authorization|bearerAuth|accessToken" milestory-frontend/src/app milestory-frontend/src/api/api/default.service.ts` returns no matches in app code and no matches in regenerated `default.service.ts`.
  </acceptance_criteria>
  <action>After running `npm run client-gen`, update `app.config.ts` to provide `provideHttpClient()`, `provideRouter(routes)`, and `provideApi('/api')` or `provideApi('http://localhost:8080')` depending on the repo's existing local routing strategy, with no auth configuration per D-11. Replace the root shell with a routed app that lazy-loads a `HomePage` component at path `''`. Create `milestory-frontend/src/app/core/status/foundation-status.store.ts` as a signal-based store/facade that injects the regenerated `DefaultService`, calls the generated `apiV1StatusGet` method on initialization and retry, and exposes explicit states `loading`, `ready`, `empty`, and `error` matching the UI spec. Add `foundation-status.store.spec.ts` covering successful load, empty payload handling, and retry after error. Do not add token storage, interceptors, guards, or auth terminology anywhere in app code per D-11.</action>
  <verify>
    <automated>cd milestory-frontend && npm run client-gen && npm run test:ci -- --include src/app/core/status/foundation-status.store.spec.ts</automated>
  </verify>
  <done>The frontend can call the backend-owned status endpoint through regenerated client code with no bearer-token plumbing and exposes a stable view model for the home screen.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Milestory home page and remove all shipped starter content</name>
  <read_first>
    .planning/phases/01-foundation-and-personal-mode/01-UI-SPEC.md
    milestory-frontend/src/app/app.html
    milestory-frontend/src/app/app.scss
    milestory-frontend/src/styles.scss
    milestory-frontend/src/app/app.spec.ts
    milestory-frontend/AGENTS.md
  </read_first>
  <files>milestory-frontend/src/app/app.html, milestory-frontend/src/app/app.scss, milestory-frontend/src/styles.scss, milestory-frontend/src/app/app.spec.ts, milestory-frontend/src/app/features/home/home.page.ts, milestory-frontend/src/app/features/home/home.page.html, milestory-frontend/src/app/features/home/home.page.scss, milestory-frontend/src/app/features/home/home.spec.ts</files>
  <acceptance_criteria>
    - `rg -n "Angular|Congratulations|Hello, milestory-frontend|Inter Tight|angular-logo" milestory-frontend/src/app milestory-frontend/src/styles.scss` returns no matches.
    - `rg -n "Milestory|Refresh status|Foundation is in place|backend|next phase" milestory-frontend/src/app/features/home/home.page.html milestory-frontend/src/app/app.spec.ts milestory-frontend/src/app/features/home/home.spec.ts` returns matches.
    - `rg -n "Fraunces|Manrope|#F6F1E7|#E6DDCF|#1F6B5C|loading|ready|empty|error" milestory-frontend/src/styles.scss milestory-frontend/src/app/features/home/home.page.*` returns matches.
    - `cd milestory-frontend && npm run test:ci` exits `0`.
  </acceptance_criteria>
  <action>Delete the Angular starter markup from `app.html` and reduce `App` to a router host only. Move the UI tokens into `src/styles.scss`: import `Fraunces` and `Manrope`, declare CSS custom properties for the warm palette from the UI spec, and set accessible base typography. Create `features/home/home.page.ts|html|scss` as a standalone OnPush page using semantic `main`, `header`, and `section` landmarks. The page must render exactly three zones per D-05 through D-08: a brand intro with the Milestory name, one-sentence promise, and primary button labeled `Refresh status`; a foundation status card that renders the four explicit store states; and a build-notes strip with 2-3 short bullets that say goal planning and deeper dashboards arrive in later phases without overselling. Keep copy free of `personal mode`, `sign in`, `account`, `token`, and CRUD placeholder language per D-01, D-04, and D-11. Replace `app.spec.ts` starter assertions with Milestory shell assertions and add `home.spec.ts` coverage for loading/ready/error rendering.</action>
  <verify>
    <automated>cd milestory-frontend && npm run test:ci</automated>
  </verify>
  <done>The shipped frontend is a Milestory-branded status experience that reads backend data, honors the UI contract, and contains no visible starter or auth scaffolding.</done>
</task>

</tasks>

<verification>
Run the frontend locally and confirm `/` shows the three-zone Milestory layout, the primary CTA reloads the status card, and the page remains readable on desktop and mobile widths with no auth copy.
</verification>

<success_criteria>
The user lands on a credible Milestory home surface that loads backend status immediately, presents only the narrow Phase 1 information architecture, and keeps all domain/status logic on the backend.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-personal-mode/01-03-SUMMARY.md`
</output>
