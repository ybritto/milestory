---
phase: 01-foundation-and-personal-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - milestory-api/rest/api-v1.yaml
  - milestory-api/rest/paths/status.yaml
  - milestory-api/rest/schemas/status.yaml
  - milestory-backend/src/main/resources/openapi-processor-mapping.yaml
  - milestory-frontend/package.json
  - README.md
autonomous: true
requirements:
  - PLAT-01
must_haves:
  truths:
    - "User can use the Phase 1 stack without any sign-in or bearer token requirement."
    - "The repository and generated client describe Milestory instead of a generic template where that affects current Phase 1 delivery."
    - "The contract exposes only a narrow foundation/status surface for the frontend home screen."
  artifacts:
    - path: "milestory-api/rest/api-v1.yaml"
      provides: "Auth-free root contract with status path registration"
      min_lines: 12
    - path: "milestory-api/rest/paths/status.yaml"
      provides: "Single GET status endpoint definition"
      contains: "tags:"
    - path: "milestory-api/rest/schemas/status.yaml"
      provides: "Status projection schema for the Phase 1 home screen"
      contains: "FoundationStatusResponse"
    - path: "README.md"
      provides: "Milestory-specific project setup and Phase 1 positioning"
      contains: "Milestory"
  key_links:
    - from: "milestory-api/rest/api-v1.yaml"
      to: "milestory-api/rest/paths/status.yaml"
      via: "paths registration"
      pattern: "/api/v1/status"
    - from: "milestory-api/rest/schemas/status.yaml"
      to: "milestory-backend/src/main/resources/openapi-processor-mapping.yaml"
      via: "generated Spring models package"
      pattern: "com.ybritto.milestory.generated"
    - from: "milestory-api/rest/api-v1.yaml"
      to: "milestory-frontend/src/api"
      via: "npm run client-gen"
      pattern: "client-gen"
---

<objective>
Make the API contract and repo-facing Phase 1 materials honestly auth-free and Milestory-branded, so downstream backend and frontend work start from the correct source of truth.

Purpose: Implement D-09, D-10, D-12, D-13, D-14, and D-15 before any generated code or runtime work builds on the wrong assumptions.
Output: A narrow `/api/v1/status` contract with concrete status schemas, no global bearer auth, and updated top-level docs/scripts metadata that describe Milestory rather than a reusable template.
</objective>

<execution_context>
@/Users/ybritto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ybritto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-personal-mode/01-CONTEXT.md
@.planning/phases/01-foundation-and-personal-mode/01-RESEARCH.md
@.planning/phases/01-foundation-and-personal-mode/01-UI-SPEC.md
@AGENTS.md
@milestory-api/AGENTS.md
@milestory-frontend/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the scaffold contract with an auth-free Phase 1 status surface</name>
  <read_first>
    .planning/phases/01-foundation-and-personal-mode/01-CONTEXT.md
    .planning/phases/01-foundation-and-personal-mode/01-RESEARCH.md
    milestory-api/rest/api-v1.yaml
    milestory-api/rest/paths/hello.yaml
    milestory-api/AGENTS.md
  </read_first>
  <files>milestory-api/rest/api-v1.yaml, milestory-api/rest/paths/status.yaml, milestory-api/rest/schemas/status.yaml</files>
  <acceptance_criteria>
    - `rg -n "security:|bearerAuth" milestory-api/rest/api-v1.yaml` returns no matches per D-10.
    - `rg -n "/api/v1/status" milestory-api/rest/api-v1.yaml milestory-api/rest/paths/status.yaml` returns matches in both files.
    - `rg -n "tags:|FoundationStatusResponse|mode|database|migration" milestory-api/rest/paths/status.yaml milestory-api/rest/schemas/status.yaml` returns matches proving the narrow status endpoint and schema fields exist per D-12.
    - `rg -n "/api/v1/hello" milestory-api/rest` returns no matches.
  </acceptance_criteria>
  <action>Create `milestory-api/rest/paths/status.yaml` with one tagged `GET /api/v1/status` operation named for foundation status, not hello-world. The `200` response must reference `../schemas/status.yaml#/FoundationStatusResponse` and the `503` response must reference `../schemas/status.yaml#/FoundationStatusUnavailableResponse`. Create `milestory-api/rest/schemas/status.yaml` with `FoundationStatusResponse` containing at least `headline` (string), `summary` (string), `mode` (enum with `ready`, `empty`, `degraded`), `apiVersion` (string), `database` (object with `status` and `name`), `migration` (object with `status` and `baseline`), `generatedAt` (string date-time), and `notes` (array of strings). Remove the global `security` block and `components.securitySchemes.bearerAuth` from `milestory-api/rest/api-v1.yaml` per D-09 and D-10, register only `/api/v1/status`, and keep `openapi: 3.1.2` unchanged.</action>
  <verify>
    <automated>rg -n "security:|bearerAuth|/api/v1/status|FoundationStatusResponse|FoundationStatusUnavailableResponse" milestory-api/rest/api-v1.yaml milestory-api/rest/paths/status.yaml milestory-api/rest/schemas/status.yaml</automated>
  </verify>
  <done>The API module defines exactly the auth-free Phase 1 status contract needed by downstream generation and no longer advertises bearer-auth behavior that the product does not support.</done>
</task>

<task type="auto">
  <name>Task 2: Align generation and product-facing docs with Milestory Phase 1 reality</name>
  <read_first>
    .planning/phases/01-foundation-and-personal-mode/01-CONTEXT.md
    README.md
    milestory-backend/src/main/resources/openapi-processor-mapping.yaml
    milestory-frontend/package.json
    AGENTS.md
  </read_first>
  <files>README.md, milestory-backend/src/main/resources/openapi-processor-mapping.yaml, milestory-frontend/package.json</files>
  <acceptance_criteria>
    - `rg -n "Project Template|template-api|template-backend|template-frontend" README.md` returns no matches.
    - `rg -n "Milestory|Phase 1|foundation" README.md` returns matches showing the README is Milestory-specific per D-13 through D-15.
    - `rg -n "package-name: com.ybritto.milestory.generated" milestory-backend/src/main/resources/openapi-processor-mapping.yaml` returns one match.
    - `rg -n "\"client-gen\"" milestory-frontend/package.json` returns one match and still points to `../milestory-api/rest/api-v1.yaml`.
  </acceptance_criteria>
  <action>Rewrite `README.md` so the repository reads as Milestory, not a generic bootstrap template, while still keeping the known-template-leftovers concern visible per D-13 and D-15. The README must describe the three real modules `milestory-api`, `milestory-backend`, and `milestory-frontend`, local PostgreSQL via `compose.yaml`, and the Phase 1 boundary: auth-free personal use now, goals/progress later. Keep `milestory-backend/src/main/resources/openapi-processor-mapping.yaml` package root at `com.ybritto.milestory.generated`; remove the unused `/response` path mapping block so the file only expresses the generated package/model policy needed for the new status contract. In `milestory-frontend/package.json`, keep the package name `milestory-frontend` and the `client-gen` script path to `../milestory-api/rest/api-v1.yaml`, but ensure no script or metadata refers to template modules or auth-specific setup.</action>
  <verify>
    <automated>rg -n "Project Template|template-api|template-backend|template-frontend|Milestory|Phase 1|package-name: com.ybritto.milestory.generated|client-gen" README.md milestory-backend/src/main/resources/openapi-processor-mapping.yaml milestory-frontend/package.json</automated>
  </verify>
  <done>The contract-generation metadata and top-level repo guidance now describe the actual Milestory Phase 1 product and no longer push executors toward template or auth-first assumptions.</done>
</task>

</tasks>

<verification>
Run `npm run client-gen` from `milestory-frontend` after this plan to regenerate an auth-free client, then confirm no bearer token hooks remain in `src/api`.
</verification>

<success_criteria>
Phase 1 has a single, narrow, auth-free status contract; repo-facing docs describe Milestory honestly; and downstream backend/frontend plans can implement against the cleaned contract without discovering hidden auth/template leftovers.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-personal-mode/01-01-SUMMARY.md`
</output>
