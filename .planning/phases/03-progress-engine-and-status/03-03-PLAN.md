---
phase: 03-progress-engine-and-status
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts
  - milestory-frontend/src/app/features/goals/shared/goal-planning.store.spec.ts
  - milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts
  - milestory-frontend/src/app/features/goals/detail/goal-detail.page.html
  - milestory-frontend/src/app/features/goals/detail/goal-detail.page.scss
  - milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts
  - milestory-frontend/src/styles.scss
autonomous: true
requirements: [PROG-01, PROG-02, PROG-03, PROG-04]
must_haves:
  truths:
    - "The goal detail page shows status first, then actual-so-far versus expected-by-today, then visible history, then checkpoint context per D-09 through D-16."
    - "The user can record a progress update from the goal detail route without navigating away and returns to refreshed detail state on success."
    - "Archived goals remain readable but do not expose a working add-progress action."
  artifacts:
    - path: "milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts"
      provides: "Generated-client orchestration for progress save, detail refresh, and success/error messaging."
    - path: "milestory-frontend/src/app/features/goals/detail/goal-detail.page.html"
      provides: "Status hero, comparison strip, progress history, and checkpoint section in the UI-spec order."
    - path: "milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts"
      provides: "Unit coverage for status rendering, archived read-only behavior, and overlay-triggered progress flow."
  key_links:
    - from: "milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts"
      to: "milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts"
      via: "signal-based detail state and progress-submit methods"
      pattern: "recordProgress|successMessage|goal\\("
    - from: "milestory-frontend/src/app/features/goals/detail/goal-detail.page.html"
      to: "milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts"
      via: "copy and order assertions"
      pattern: "Add progress update|Actual so far|Expected by today|No progress logged yet"
---

<objective>
Ship the Phase 3 goal-detail experience with progress entry, backend-owned pace rendering, visible history, and read-only archive behavior.

Purpose: Deliver the user-facing tracking core without introducing frontend-owned calculation logic or a separate progress screen.
Output: Updated store, goal-detail page, styling, and frontend tests covering the overlay workflow and status rendering.
</objective>

<execution_context>
@/Users/ybritto/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ybritto/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-progress-engine-and-status/03-CONTEXT.md
@.planning/phases/03-progress-engine-and-status/03-UI-SPEC.md
@.planning/phases/03-progress-engine-and-status/03-VALIDATION.md
@.planning/phases/03-progress-engine-and-status/03-02-PLAN.md
@milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts
@milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts
@milestory-frontend/src/app/features/goals/detail/goal-detail.page.html
@milestory-frontend/src/app/features/goals/detail/goal-detail.page.scss
@milestory-frontend/src/styles.scss

<interfaces>
From `milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts`:

```ts
readonly goal = this.goalState.asReadonly();
readonly goalCategories = this.categoriesState.asReadonly();
readonly viewState = this.viewStateState.asReadonly();
loadGoal(goalId: string): void;
archiveGoal(goalId: string): Observable<ArchiveGoal200Response>;
```

From `milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts`:

```ts
readonly goal = this.goalPlanningStore.goal;
readonly categories = this.goalPlanningStore.goalCategories;
readonly checkpointCards = computed(() => this.goal()?.checkpoints ?? []);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the goal planning store for progress entry, refresh, and success feedback</name>
  <files>milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts, milestory-frontend/src/app/features/goals/shared/goal-planning.store.spec.ts</files>
  <read_first>
- milestory-frontend/src/app/features/goals/shared/goal-planning.store.ts
- milestory-frontend/src/app/features/goals/shared/goal-planning.store.spec.ts
- milestory-frontend/src/api/api/goals.service.ts
- .planning/phases/03-progress-engine-and-status/03-02-PLAN.md
- milestory-frontend/AGENTS.md
  </read_first>
  <behavior>
    - Test 1: saving a progress update calls the generated `GoalsService` progress-entry method, reloads the goal detail, and exposes the success message `Progress updated. Your status has been refreshed.`
    - Test 2: when the backend returns `entryType` equal to `CORRECTION`, the store exposes `Correction saved. Milestory updated your status from the new total.`
    - Test 3: opening a progress overlay for an archived goal is blocked by state rather than silently failing.
  </behavior>
  <action>Keep the store backend-driven. Add signal state for overlay visibility, submission state, and one transient success message. Add a method named `recordProgress(goalId: string, request: RecordGoalProgressEntryRequest)` that calls the generated progress-entry endpoint, then immediately calls `loadGoal(goalId)` so the refreshed detail payload stays authoritative per D-15. Use the backend-returned `entryType` to choose exactly one of the two UI-spec success strings. Clear the success message when reopening the overlay or loading a different goal. Do not derive pace status, expected progress, or correction state in Angular.</action>
  <acceptance_criteria>
- `goal-planning.store.ts` contains the exact strings `recordProgress`, `Progress updated. Your status has been refreshed.`, and `Correction saved. Milestory updated your status from the new total.`
- `goal-planning.store.spec.ts` asserts that `GoalsService` progress-entry submission is followed by a goal reload call.
- The store exposes a read-only success-message signal or equivalent computed state consumed by the page.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix milestory-frontend test -- --watch=false --include='src/app/features/goals/shared/goal-planning.store.spec.ts'</automated>
  </verify>
  <done>The frontend store can submit progress, refresh goal detail from the backend, and surface the exact success feedback strings required by the UI spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild goal detail around the Phase 3 status hero, comparison strip, history, and overlay workflow</name>
  <files>milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts, milestory-frontend/src/app/features/goals/detail/goal-detail.page.html, milestory-frontend/src/app/features/goals/detail/goal-detail.page.scss, milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts, milestory-frontend/src/styles.scss</files>
  <read_first>
- milestory-frontend/src/app/features/goals/detail/goal-detail.page.ts
- milestory-frontend/src/app/features/goals/detail/goal-detail.page.html
- milestory-frontend/src/app/features/goals/detail/goal-detail.page.scss
- milestory-frontend/src/app/features/goals/detail/goal-detail.page.spec.ts
- milestory-frontend/src/styles.scss
- .planning/phases/03-progress-engine-and-status/03-UI-SPEC.md
- .planning/phases/03-progress-engine-and-status/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: rendered order is status hero, comparison strip, progress history, checkpoint section.
    - Test 2: active goals show `Add progress update`, `Actual so far`, and `Expected by today`; archived goals hide the progress CTA and show a read-only note.
    - Test 3: loading state renders skeleton blocks for the pace label, hero headline, summary sentence, and comparison strip; error state renders the exact retry copy from the UI spec and a retry action.
    - Test 4: empty progress state renders `No progress logged yet` and the exact helper body from the UI spec.
    - Test 5: opening the overlay moves focus to the dialog title or first field, `Escape` closes it, focus returns to `Add progress update`, and tab order remains trapped while open.
  </behavior>
  <action>Refactor the goal detail page to match the UI-spec order exactly: `status hero`, `plan-versus-actual comparison strip`, `progress history`, `checkpoint section`. Put the `Add progress update` trigger inside the hero for active goals only per D-13 and D-14. Implement all explicit hero states from the UI spec: `loading` must render skeleton blocks for the pace label, hero headline, summary sentence, and comparison strip; `ready-with-history` must render the full status and history flow; `ready-empty` must render the exact empty-state heading/body plus the primary add-progress action; `error` must render the exact UI-spec error copy and a retry button that calls the store reload path. Implement one responsive overlay pattern on the same route: centered modal on desktop and bottom-sheet styling on small screens, with fields ordered `Date`, `Progress so far`, `Note`, and buttons labeled `Save progress update` and `Close without saving`. Use reactive forms and explicit overlay accessibility behaviors: opening moves focus to the dialog title or first field, tab order stays trapped inside the overlay, `Escape` closes the overlay, and close always returns focus to the `Add progress update` trigger. Render backend fields directly for `paceStatus`, `paceSummary`, `paceDetail`, `currentProgressValue`, `expectedProgressValueToday`, and checkpoint annotation strings. Keep the history list newest first, show a `Correction` chip when `entryType === 'CORRECTION'`, and preserve archived goals as readable with the exact read-only note from the UI spec. Extend SCSS tokens only where necessary for Phase 3 status tints, skeleton blocks, error state, and overlay surfaces.</action>
  <acceptance_criteria>
- `goal-detail.page.html` contains the exact strings `Add progress update`, `Save progress update`, `Close without saving`, `Actual so far`, `Expected by today`, and `No progress logged yet`.
- `goal-detail.page.html` contains the exact error copy `Milestory could not load the current progress status for this goal. Retry the page, and if the issue persists confirm the backend is running and the goal still exists.` and a retry action.
- `goal-detail.page.spec.ts` asserts archived goals do not render `Add progress update`.
- `goal-detail.page.spec.ts` asserts loading skeleton output for the hero and comparison strip.
- `goal-detail.page.spec.ts` asserts the empty-state body `Add your first update to see how this goal is tracking against the plan for today.`
- `goal-detail.page.spec.ts` asserts `Escape` dismissal and focus return to the trigger, and includes at least one assertion covering trapped focus behavior while the overlay is open.
- `goal-detail.page.scss` contains styles for the overlay plus section classes for the status hero, comparison strip, progress history, and checkpoint section.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix milestory-frontend test -- --watch=false --include='src/app/features/goals/detail/goal-detail.page.spec.ts' --include='src/app/features/goals/shared/goal-planning.store.spec.ts'</automated>
  </verify>
  <done>The goal detail route now delivers the Phase 3 UX: backend-owned pace explanation, compact comparison, visible history, checkpoint context, and in-place progress entry.</done>
</task>

</tasks>

<verification>
Run the targeted frontend store and detail specs after the store task and again after the page task. Manual follow-up should confirm overlay feel, focus handling, and status tone against the seeded behind/on-pace/ahead states.
</verification>

<success_criteria>
1. The user can add a progress update from the goal detail page without navigating away.
2. The detail view explains status using backend fields only and preserves the UI-spec content order.
3. Archived goals remain readable while blocking new progress entry affordances.
</success_criteria>

<output>
After completion, create `.planning/phases/03-progress-engine-and-status/03-03-SUMMARY.md`
</output>
